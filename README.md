# Lesson's exercises

## How to:

- install dev-server
```bash
npm i -g dev-server
```
- run `dev-server`:
```bash
dev-server
```
- Open in browser
- Change any test name in the `index.html` in order to check if it pass correctly or not.

### Enjoy!